# Reasoning_Param_Injector
SillyTavern extension that stores `reasoning_effort` / `verbosity` values per model and automatically injects them into requests when you use a Custom (OpenAI-compatible) endpoint.
